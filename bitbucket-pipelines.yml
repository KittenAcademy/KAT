#  Template NodeJS build

#  This template allows you to validate your NodeJS code.
#  The workflow allows running tests and code linting on the default branch.

image: node:16

pipelines:
  default:
    - step:
        name: prep
        script:
          - npm install
        caches:
          - node
    - step:
        name: Test
        script:
          - npm install
          - mv ./src/privatesettings.ts.example.txt ./src/privatesettings.ts
          - npm test
        caches:
          - node
    - step:
        name: Lint
        script:
          - npm install
          - npm run lint
        caches:
          - node
    - step:
        name: Build
        script:
          - npm install
          - mv ./src/privatesettings.ts.example.txt ./src/privatesettings.ts
          - npm run build
        caches:
          - node
